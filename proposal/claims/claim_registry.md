# Claim–Evidence Registry

Start from `templates/nsfc/claim-registry.md` and keep it updated.

## Tagging Rule

In `proposal/source/*.md`, append claim tags to strong-claim sentences:

- Example: 我们将识别……的因果效应。[[CLAIM:C001]]

Each tag must have a matching row with `Status = BOUND`.
